[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "frameos-sdk"
version = "0.1.0"
requires-python = ">=3.11"
description = "Python client for the FrameOS OpenAPI contract"
license = "MIT"
