{
  "name": "@frameos/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "frameos": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "cli": "tsx src/index.ts",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
