[project]
name = "frameos-sdk"
version = "0.1.0"
description = "Dependency-free Python client for the FrameOS REST API"
requires-python = ">=3.11"
license = "MIT"
authors = [{ name = "FrameOS" }]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
